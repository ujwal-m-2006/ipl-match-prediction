# ---------------------------------------------------------------------------
# Runtime dependencies — everything needed to RUN the dashboard and the API.
#
# Deliberately lean: this is the list a hosting platform installs, and every
# extra package is another chance for the build to fail. Development extras
# (test runner, database drivers, migrations) live in requirements-dev.txt.
#
# Tested on Python 3.11 - 3.13. See .python-version.
# ---------------------------------------------------------------------------

# --- Core data stack --------------------------------------------------------
pandas>=2.1
numpy>=1.26
python-dateutil>=2.8

# --- Data collection --------------------------------------------------------
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9

# --- Database ---------------------------------------------------------------
# SQLAlchemy alone covers SQLite, which is what the shipped deployment uses.
# PostgreSQL and MySQL drivers are in requirements-dev.txt — install those only
# if you point IPL_DATABASE_URL at a server-backed database.
SQLAlchemy>=2.0

# --- Configuration ----------------------------------------------------------
python-dotenv>=1.0
PyYAML>=6.0

# --- Machine learning -------------------------------------------------------
scikit-learn>=1.4
joblib>=1.3
scipy>=1.11

# CatBoost is required to deserialise the trained match-winner model. If it
# fails to install, the app still starts and serves the other three models —
# see ipl.models.predict.PredictionService.has_model.
catboost>=1.2

# Compared during training and loaded only if a saved model uses them.
xgboost>=2.0
lightgbm>=4.1

# --- Visualisation ----------------------------------------------------------
plotly>=5.18
matplotlib>=3.8

# --- Dashboard --------------------------------------------------------------
# >=1.36 for the st.Page / st.navigation multi-page API.
streamlit>=1.36

# --- REST API ---------------------------------------------------------------
fastapi>=0.109
uvicorn[standard]>=0.27
